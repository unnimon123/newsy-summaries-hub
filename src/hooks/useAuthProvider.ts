
// This file is now just a re-export for backward compatibility
// This allows existing imports to continue working while we transition
// to the new, more modular structure
import { useAuthProvider } from "./auth/useAuthProvider";

export { useAuthProvider };
